import { DefaultDirective } from './default.directive';

describe('DefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultDirective();
    expect(directive).toBeTruthy();
  });
});
